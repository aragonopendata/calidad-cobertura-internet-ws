package es.ideariumConsultores.opendata.cobertura;

/**
 * Tests de contrato HTTP cuyas respuestas de servicio están mockeadas.
 * Fijan controller + framework, NO el contenido real de las respuestas
 * (eso lo cubre la Capa 3, golden master contra la app real).
 */
public interface ContratoConMocks {}
